---
phase: 01-safety-privacy-and-state-foundation
plan: "07"
type: execute
wave: 7
depends_on:
  - 01-06
files_modified:
  - safety/internal/workflow/synthetic.go
  - safety/cmd/yamc-safety/main.go
  - safety/internal/e2e/phase_e2e_test.go
  - safety/manifests/offline-suite.v1.json
  - safety/testdata/blueprints/walking-skeleton/expected-report.json
  - safety/README.md
  - safety/CLAUDE.md
  - safety/AGENTS.md
  - README.md
autonomous: true
requirements:
  - SAFE-01
  - SAFE-02
  - SAFE-03
  - SAFE-04
  - SAFE-05
  - SAFE-06
  - SAFE-07
  - SAFE-08
must_haves:
  truths:
    - "D-01～D-04：完整 phase run 重新读取并验证六类 artifact、canonical digests、封闭 class-kind-retention/expiry/terminal/transitive-pin lifecycle 与 exact lineage，而不是信任先前退出码或目录位置。"
    - "D-05～D-08：完整 phase run 对 stdout、stderr、store 与 bounded adapter capture 使用同一 six-namespace privacy boundary，并在持久化/渲染前执行 closed surface_domain compatibility table。"
    - "D-09～D-12：完整 phase run 始终 offline、fresh external、不可升级；network/live 只有 exact contract deny/manual paths。"
    - "D-13～D-16：默认 phase gate 的完整 observation window 由已证明的 exact real protected manifest 包裹，内层 synthetic evidence 不能产生 claim，只有完整 real required evidence 才通过且 claim 精确受限。"
    - "D-17～D-19：完整 report 保留 control-plane 分层和 report-only extra state；模块不存在任何真实 apply 或 destructive route。"
    - "操作者拥有同步的 root/safety 文档与实际 safety/AGENTS.md -> CLAUDE.md symlink；缺少 Go 明确为 manual-required。"
  artifacts:
    - path: "safety/manifests/offline-suite.v1.json"
      provides: "Exact full-phase suite and component bindings"
      contains: "walking-skeleton"
    - path: "safety/internal/e2e/phase_e2e_test.go"
      provides: "Cross-cutting SAFE-01..08 full phase positive/negative proof"
      contains: "phase-e2e"
    - path: "safety/README.md"
      provides: "Operator CLI, tier, artifact, claim and manual-required documentation"
      contains: "covered-surfaces-unchanged-for-run"
    - path: "safety/CLAUDE.md"
      provides: "Local implementation, safety, test and documentation rules"
      contains: "禁止"
    - path: "safety/AGENTS.md"
      provides: "Symlink to local guidance"
    - path: "README.md"
      provides: "Root config table, setup/test entry and local-state boundary"
      contains: "safety"
  key_links:
    - from: "safety/manifests/offline-suite.v1.json"
      to: "safety/internal/workflow/synthetic.go"
      via: "exact suite/component IDs and manifest digests"
      pattern: "suite"
    - from: "safety/scripts/test.sh"
      to: "safety/internal/e2e/phase_e2e_test.go"
      via: "phase gate"
      pattern: "phase"
    - from: "safety/AGENTS.md"
      to: "safety/CLAUDE.md"
      via: "relative symlink"
      pattern: "CLAUDE.md"
---

## Phase Goal

**As a** Mac 配置仓库操作者，**I want to** 校验严格区分且有 lineage 的恢复 artifacts，并在隔离 fixture 中运行端到端安全测试，**so that** 我能证明本次运行覆盖的真实 Mac 表面没有发生未经授权的变化。

<objective>
收束同一 vertical slice：以 exact offline suite 把六类 artifact、privacy、fixture/tier、sentinel 与 control-plane/no-cleanup contract 串成一个 fresh external phase run，并交付完整 operator/maintainer 文档。

Purpose: 用一个可重复、低延迟、无 live mutation 的 command 证明 SAFE-01～SAFE-08 与 D-01～D-19 共同成立，同时完成项目文档、local guidance、symlink、隐私审查与原子英文提交要求。
Output: full-phase E2E、exact suite manifest、expected bounded report、整合后的 CLI/workflow、`safety/README.md`、`safety/CLAUDE.md`、真实 `safety/AGENTS.md -> CLAUDE.md` symlink 与 root README 更新。
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.ai/DECISIONS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-safety-privacy-and-state-foundation/01-CONTEXT.md
@.planning/phases/01-safety-privacy-and-state-foundation/01-RESEARCH.md
@.planning/phases/01-safety-privacy-and-state-foundation/01-PATTERNS.md
@.planning/phases/01-safety-privacy-and-state-foundation/01-VALIDATION.md
@.planning/phases/01-safety-privacy-and-state-foundation/SKELETON.md
@.planning/phases/01-safety-privacy-and-state-foundation/01-06-SUMMARY.md

<interfaces>
- Stable operator commands are `validate`, `store`, `fixture run`, `sentinel verify`, and `report`; there is no auth, HTTP, daemon or apply command.
- `offline-suite.v1.json` binds exact task groups, protected/network manifests, test tier and expected claim; phase execution rejects suite/manifest drift.
- Full report is reconstructed only from artifacts loaded by exact digest and revalidated through kind, privacy, lineage, sentinel and policy contracts.
- Full report surface IDs are fixed to the existing namespace registry: `repo:sentinel/worktree/{tracked,index}`, `home:.zshrc`, `home:sentinel/manager/mise-data`, `profile:sentinel/service/homebrew-mxcl-nginx`, and `profile:sentinel/named-target/system-shells`; domain, namespace and resolver compatibility is revalidated before rendering.
- The full phase order is fixed: validate exact adapter proofs/manifest → real-before → create/run isolated fixture plus inner synthetic sentinels → freeze primary workload verdict → marker-owned fixture teardown unless pre-run keep → real-after → monotonic final combine.
- Documentation treats actual external local-state paths as runtime-resolved logical refs; no in-repository ignored state is introduced, so `.gitignore` and `.gitleaks.toml` are audited but remain unchanged unless a narrowly demonstrated fixture requires an exact exception.
</interfaces>
</context>

<execution_rules>
- Full phase verification is offline and may invoke only Plan 01-05's exact-manifest, pre-proven read-only outer sentinels; these are not tool discovery or `live-check`. It may not invoke Nix, Homebrew, manager/bootstrap/install/download, app reload/validator, network, trust, destructive/convergence cleanup, link or host apply.
- The only allowed teardown is the external marker/UID/nonce/containment-verified fixture child after primary verdict freeze. It cannot restore real state, delete retention base/arbitrary/live state, or turn non-pass into pass.
- If local Go is missing, stop with manual-required; do not modify PATH through a manager or bootstrap anything.
- Preserve dirty `CLAUDE.md`, `.ai/`, `.config/alma/`; never inspect `.config/alma/`. Use exact path lists for staging and staged privacy review, English commits, no push.
- All new/modified script comments are Chinese. Root README and local safety docs must stay synchronized with actual commands and limits.
</execution_rules>

<tasks>

<task type="auto" tdd="true">
  <name>Task 01-07-01: 绑定 exact suite 并让 full phase E2E 通过</name>
  <files>safety/internal/workflow/synthetic.go, safety/cmd/yamc-safety/main.go, safety/internal/e2e/phase_e2e_test.go, safety/manifests/offline-suite.v1.json, safety/testdata/blueprints/walking-skeleton/expected-report.json</files>
  <read_first>
    - AGENTS.md
    - .ai/DECISIONS.md
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - .planning/phases/01-safety-privacy-and-state-foundation/01-CONTEXT.md
    - .planning/phases/01-safety-privacy-and-state-foundation/01-RESEARCH.md
    - .planning/phases/01-safety-privacy-and-state-foundation/01-PATTERNS.md
    - .planning/phases/01-safety-privacy-and-state-foundation/01-VALIDATION.md
    - safety/scripts/test.sh
    - safety/internal/artifact/envelope.go
    - safety/internal/artifact/lineage.go
    - safety/internal/privacy/gate.go
    - safety/internal/privacy/capture.go
    - safety/internal/fixture/root.go
    - safety/internal/fixture/network.go
    - safety/internal/sentinel/verdict.go
    - safety/internal/sentinel/real.go
    - safety/manifests/real-adapters.v1.json
    - safety/internal/contract/controlplane.go
    - safety/internal/contract/policy.go
    - safety/internal/workflow/synthetic.go
    - safety/cmd/yamc-safety/main.go
    - safety/internal/e2e/phase_e2e_test.go (new; confirm absent before creation)
    - safety/manifests/offline-suite.v1.json (new; confirm absent before creation)
    - safety/testdata/blueprints/walking-skeleton/expected-report.json (new; confirm absent before creation)
  </read_first>
  <behavior>
    - `./safety/scripts/test.sh phase` validates real adapter proofs, captures real-before, creates/runs a fresh external fixture with inner synthetic sentinels, freezes primary verdict, performs allowed fixture teardown unless pre-run keep, captures real-after, then combines and reports exact suite/manifest digests.
    - Report reverse-validation proves all six kinds and lineage, privacy-clean sinks, isolated roots, offline tier, strict sentinel pass and report-only extra state.
    - Expected report records inner status `synthetic-sentinel-passed`, the exact compatible public surface refs and opaque real tokens; only complete outer evidence adds `covered-surfaces-unchanged-for-run`.
    - Cross-cutting negative matrix covers kind confusion, stale lineage, canary/raw leakage, fixture escape, tier/network/live escalation, incomplete/different sentinel, duplicate owner and destructive operation.
    - Every non-pass remains non-zero; phase run never retries, downgrades optionality, performs live/destructive convergence cleanup, or escalates. Marker-owned fixture teardown is the sole exception and only preserves/worsens the frozen verdict.
  </behavior>
  <action>
Write the full phase E2E and cross-cutting negative matrix first, then bind the exact offline suite manifest to task groups, `offline-static`/offline isolated tier, protected-surface/real-adapter/network-contract digests and expected claim. Integrate D-01/D-02/D-03/D-04 by reloading all six artifacts from exact content digests and revalidating kind, storage class, closed retention, 24-hour expiry, plan terminal state, transitive pins, provenance, canonical digest and lineage, including wrong-class/stale/overwrite/premature-delete/unsupported-retention negatives. Integrate D-05/D-06/D-07/D-08 through the shared privacy gate and revalidate the closed domain compatibility table before persistence/rendering: worktree uses `repo:sentinel/worktree/{tracked,index}`, named-home uses `home:.zshrc`, manager-root uses `home:sentinel/manager/mise-data`, service uses `profile:sentinel/service/homebrew-mxcl-nginx`, and named-target uses `profile:sentinel/named-target/system-shells`; wrong-domain namespace, unknown namespace/domain, traversal, absolute suffix and resolver escape fail closed. Integrate D-09/D-10/D-11/D-12 with fresh external roots and offline exact-ID behavior. Integrate D-13/D-14/D-15/D-16 in the mandatory real-before/workload/freeze/teardown/real-after/combine order. Preserve Wave 1's `synthetic-sentinel-passed` contract unchanged; missing proof/real evidence is non-zero and only complete outer evidence first adds `covered-surfaces-unchanged-for-run`. Ensure expected report contains those exact public refs, opaque tokens, inner synthetic status, digests, bounded statuses and the final scoped claim—never roots, UID, identity, raw output, resolver mapping or HMAC key. Integrate D-17/D-18/D-19 as report-only policy, run gates under 60 seconds, stage exactly these five files, and create English commit `feat(01-07): integrate offline safety phase` after scoped privacy review.
  </action>
  <verify>
    <automated>./safety/scripts/test.sh task phase-e2e</automated>
  </verify>
  <acceptance_criteria>
    - Full phase exits 0 only when every component, lifecycle recheck and required outer real sentinel is green; missing/stale adapter proof and all other verdicts are non-zero.
    - Report can be reverse-validated from exact store digests and contains all six kinds, fresh evidence and report-only extra state.
    - Canaries, raw bytes, physical roots, real identity/provider/environment data and stronger claims are absent from stdout, stderr and artifacts.
    - Expected-report tests assert every exact namespace/domain mapping and reject wrong-domain, unknown, traversal, absolute-suffix and resolver-escape substitutions before rendering.
    - Success, failure and pre-run keep tests prove exact ordering; no live/destructive cleanup occurs, and the sole marker-owned fixture teardown cannot restore real state, delete arbitrary/live state or improve a frozen verdict.
    - All D-01..D-19 negative cases are exercised by named task suites or the cross-cutting matrix; no contract is documentation-only.
  </acceptance_criteria>
  <done>The operator has one complete offline command that revalidates the entire safety vertical slice and yields only evidence-supported output.</done>
</task>

<task type="auto">
  <name>Task 01-07-02: 同步 operator 文档、local guidance 与实际 AGENTS symlink</name>
  <files>safety/README.md, safety/CLAUDE.md, safety/AGENTS.md, README.md</files>
  <read_first>
    - AGENTS.md
    - README.md
    - .gitignore
    - .gitleaks.toml
    - .planning/PROJECT.md
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - .planning/phases/01-safety-privacy-and-state-foundation/01-CONTEXT.md
    - .planning/phases/01-safety-privacy-and-state-foundation/SKELETON.md
    - safety/scripts/test.sh
    - safety/cmd/yamc-safety/main.go
    - safety/README.md (new; confirm absent before creation)
    - safety/CLAUDE.md (new; confirm absent before creation)
    - safety/AGENTS.md (new symlink; confirm absent before creation)
  </read_first>
  <action>
Create `safety/README.md` documenting the five CLI commands, six artifact kinds/closed lifecycle/lineage, the exact six persistent namespaces, separate five-value `surface_domain` compatibility table, three non-escalating tiers, external root/store, retention marker/TTL, exact network/live deny contracts, outer real versus inner synthetic sentinel semantics, adapter proof freshness, mandatory sequence, four verdicts, `manual-required`, report-only extras, auth prohibition and claim ceiling. Document that Wave 1 emits only `synthetic-sentinel-passed` and only complete outer real evidence can emit `covered-surfaces-unchanged-for-run`. Explicitly distinguish forbidden live/destructive convergence cleanup from the sole post-verdict marker-owned external fixture teardown. Create `safety/CLAUDE.md` with matching Chinese guidance, actual `safety/AGENTS.md -> CLAUDE.md`, and update root `README.md` with table/test/local-state boundaries. Audit `.gitignore`/`.gitleaks.toml` without broad exceptions. Run full phase and structural checks, stage exactly the four task paths, review privacy output, and create English commit `docs(01-07): document safety control plane` without pushing.
  </action>
  <verify>
    <automated>./safety/scripts/test.sh task docs-and-phase-gate</automated>
  </verify>
  <acceptance_criteria>
    - Root README includes the safety subsystem table row, exact local offline command and external/local-state privacy boundary.
    - `safety/README.md` matches actual CLI/tier/verdict/claim behavior and explicitly rejects whole-Mac/current-host/fresh-install claims.
    - `safety/CLAUDE.md` contains the local docs/test/privacy/commit checklist and prohibited live command list.
    - `safety/AGENTS.md` is a symlink with target exactly `CLAUDE.md`, not a regular file.
    - `.gitignore` and `.gitleaks.toml` remain unchanged unless a separate narrow, evidenced need is identified; no broad allowlist/ignore hides unsafe fixture data.
    - Staged paths equal exactly `README.md`, `safety/README.md`, `safety/CLAUDE.md`, and `safety/AGENTS.md`; scoped privacy/Gitleaks review finds no leak.
  </acceptance_criteria>
  <done>The safety subsystem is discoverable, accurately documented and governed by local instructions, with a verified symlink and complete offline phase gate.</done>
</task>

</tasks>

<threat_model>
## ASVS L1 Security Posture

Defense in depth across validation, sensitive-data handling, filesystem/process isolation, least privilege, evidence integrity and secure documentation is a blocking ASVS L1 requirement. Any unresolved HIGH threat blocks phase completion.

## Trust Boundaries

| Boundary | Untrusted crossing | Control |
|---|---|---|
| Suite manifest → runner | task/component/manifest selection | exact IDs/digests and closed offline dispatch |
| All component outputs → report | artifacts/errors/evidence | full revalidation plus common privacy gate |
| Runner → local machine | process/env/filesystem capabilities | external roots, offline allowlist, no live route |
| Docs → operator action | command/claim guidance | commands verified against CLI; explicit prohibitions/claim ceiling |

## STRIDE Threat Register

| Threat ID | STRIDE | Severity | Component | Disposition | Concrete mitigation and blocking proof |
|---|---|---:|---|---|---|
| T-01 | Spoofing / Tampering | HIGH | suite and artifact graph | mitigate | Exact suite/manifest/content digests and full six-kind reverse validation; substitution matrix blocks completion. |
| T-02 | Information Disclosure | HIGH | all sinks/docs/testdata | mitigate | Common privacy gate, canary scan, logical refs, scoped Gitleaks review and no broad allowlist. |
| T-03 | Tampering / Elevation of Privilege | HIGH | CLI/dependency graph | mitigate | Closed non-apply commands, forbidden mutable policy and source-structure tests. |
| T-04 | Tampering / Elevation of Privilege | HIGH | full phase filesystem | mitigate | Fresh external roots, blank allowlisted environment, containment and no generated worktree state. |
| T-05 | Elevation of Privilege / Information Disclosure | HIGH | network/live/toolchain/outer sentinels | mitigate | Local Go/offline variables, exact deny/manual contracts, and only current-proof bounded read-only sentinel adapters; no manager/bootstrap/discovery path. |
| T-06 | Repudiation / Denial of Service | HIGH | phase verdict/claim | mitigate | Exact evidence chain, strict four-state exits, bounded execution and only the scoped claim. |

## Security Gate

Phase completion is blocked by any missing SAFE/D contract test, privacy leak, outside-root write, actual network/live command, apply/destructive route, incomplete required evidence, inaccurate documentation or stronger claim.
</threat_model>

<verification>
- `./safety/scripts/test.sh task phase-e2e`
- `./safety/scripts/test.sh task docs-and-phase-gate`
- `./safety/scripts/test.sh wave phase-integration`
- `./safety/scripts/test.sh phase`
- `test -L safety/AGENTS.md && test "$(readlink safety/AGENTS.md)" = "CLAUDE.md"`
- `git diff --check -- safety/internal/workflow/synthetic.go safety/cmd/yamc-safety/main.go safety/internal/e2e/phase_e2e_test.go safety/manifests/offline-suite.v1.json safety/testdata/blueprints/walking-skeleton/expected-report.json safety/README.md safety/CLAUDE.md safety/AGENTS.md README.md`
</verification>

<success_criteria>
- SAFE-01～SAFE-08 and D-01～D-19 are exercised in one exact offline phase gate plus task-specific negative suites.
- `./safety/scripts/test.sh phase` completes within 60 seconds when local Go and all current adapter proofs exist, writes only to fresh external roots, requires outer real evidence and emits only the scoped claim.
- Root and local documentation, actual symlink, privacy review and exact-file English commits satisfy repository guidance.
- No `.gitignore`/Gitleaks broad exception, no host deployment, no live mutation and no push occurs.
</success_criteria>

<output>
Create `.planning/phases/01-safety-privacy-and-state-foundation/01-07-SUMMARY.md` when done.
</output>
