---
phase: 01-safety-privacy-and-state-foundation
plan: "05"
type: execute
wave: 5
depends_on:
  - 01-04
files_modified:
  - safety/internal/sentinel/manifest.go
  - safety/internal/sentinel/snapshot.go
  - safety/internal/sentinel/sentinel_test.go
  - safety/manifests/protected-surfaces.v1.json
  - safety/cmd/yamc-safety/main.go
  - safety/internal/sentinel/verdict.go
  - safety/internal/sentinel/verdict_test.go
  - safety/internal/workflow/synthetic.go
  - safety/internal/e2e/sentinel_cli_test.go
  - safety/internal/sentinel/real.go
  - safety/internal/sentinel/real_test.go
  - safety/manifests/real-adapters.v1.json
  - safety/internal/e2e/real_sentinel_cli_test.go
autonomous: true
requirements:
  - SAFE-06
  - SAFE-07
must_haves:
  truths:
    - "D-08/D-13：默认 offline phase gate 在 isolated workload 外层用五个 closed surface_domain 及 namespace/domain compatibility table 观察真实 tracked worktree/index、明确 non-secret HOME entry、named manager root、controlled service 与 repo-external named target；public refs 只使用既有六 namespace，synthetic adapters 不能产生真实声明。"
    - "D-14：required evidence 完整且一致才是 passed；violation、indeterminate、harness-error 均 non-zero，只有预声明 optional 可 warning。"
    - "D-15：required surface 的任何差异只报告 change-detected-during-window；不会归因、restore、ignore、retry 或事后降级。"
    - "D-16：成功 evidence 绑定 exact suite/tier/manifest/window/before-after/excluded-optional，并只声明 covered-surfaces-unchanged-for-run。"
  artifacts:
    - path: "safety/manifests/protected-surfaces.v1.json"
      provides: "Tracked logical protected-surface scope and required/optional/excluded policy"
      contains: "named-target"
    - path: "safety/internal/sentinel/snapshot.go"
      provides: "Bounded privacy-safe synthetic domain snapshots"
      contains: "Snapshot"
    - path: "safety/internal/sentinel/verdict.go"
      provides: "Strict four-state verdict and scoped claim"
      contains: "harness-error"
    - path: "safety/internal/e2e/sentinel_cli_test.go"
      provides: "CLI exit/claim/overclaim negative proof"
      contains: "sentinel-verdicts"
    - path: "safety/manifests/real-adapters.v1.json"
      provides: "Versioned exact read-only adapter semantics, freshness and isolated negative-evidence allowlist"
      contains: "valid_until"
    - path: "safety/internal/sentinel/real.go"
      provides: "Outer real-before/real-after sentinel envelope for all five D-13 domains"
      contains: "RealEnvelope"
  key_links:
    - from: "safety/cmd/yamc-safety/main.go"
      to: "safety/internal/sentinel/manifest.go"
      via: "sentinel verify manifest load"
      pattern: "sentinel"
    - from: "safety/internal/sentinel/snapshot.go"
      to: "safety/internal/sentinel/verdict.go"
      via: "before/after evidence comparison"
      pattern: "Verdict"
    - from: "safety/internal/sentinel/verdict.go"
      to: "safety/internal/workflow/synthetic.go"
      via: "workflow report claim gate"
      pattern: "covered-surfaces-unchanged-for-run"
    - from: "safety/manifests/real-adapters.v1.json"
      to: "safety/internal/sentinel/real.go"
      via: "exact adapter/version semantics and negative-evidence gate"
      pattern: "valid_until"
---

## Phase Goal

**As a** Mac 配置仓库操作者，**I want to** 校验严格区分且有 lineage 的恢复 artifacts，并在隔离 fixture 中运行端到端安全测试，**so that** 我能证明本次运行覆盖的真实 Mac 表面没有发生未经授权的变化。

<objective>
把 sentinel 扩展为 manifest-scoped protected-surface contract、内层 synthetic test doubles、默认 phase gate 外层真实只读 before/after envelope、严格四态 verdict 与 exact evidence/claim binding。

Purpose: 落实 SAFE-07 与 D-13～D-16，让 incomplete observation、surface drift 与 harness failure 都无法被误报为安全通过。
Output: protected manifest、synthetic/real domain adapters、versioned adapter proof manifest、CLI `sentinel verify`、four-state verdict、evidence binding 与 overclaim negative tests。
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-safety-privacy-and-state-foundation/01-CONTEXT.md
@.planning/phases/01-safety-privacy-and-state-foundation/01-RESEARCH.md
@.planning/phases/01-safety-privacy-and-state-foundation/01-PATTERNS.md
@.planning/phases/01-safety-privacy-and-state-foundation/01-VALIDATION.md
@.planning/phases/01-safety-privacy-and-state-foundation/01-04-SUMMARY.md

<interfaces>
- `sentinel.Manifest` declares suite/test ID, logical surface domain/ID, required|optional|excluded, adapter ID and bounds before observation begins.
- `sentinel.Snapshotter` returns typed status plus opaque token; inner workload tests use injected synthetic resolvers, while the default phase gate separately requires the exact allowlisted real adapters.
- `sentinel.Evaluate` returns exactly `passed`, `violation`, `indeterminate`, or `harness-error` and an exit class.
- `sentinel.Evidence` binds suite/tier/manifest/window and per-surface before/after tokens; the HMAC comparison key is per-run and never persisted.
- `sentinel.RealAdapterRegistry` accepts an adapter/version only when `real-adapters.v1.json` records current official read-only semantics (`reviewed_at`, at most 30-day `valid_until`) and a passing isolated negative-suite ID/digest for that exact version. Missing/stale proof is `indeterminate`/`manual-required`, non-zero.
- Public artifacts contain only logical IDs and opaque per-run tokens; physical roots, UID, identity and the ephemeral HMAC key remain process memory only.
- Surface refs must pass Plan 01-03's closed compatibility table: `worktree` → `repo:sentinel/worktree/{tracked,index}`, `named-home` → `home:.zshrc`, `manager-root` → `home:sentinel/manager/mise-data`, `service` → `profile:sentinel/service/homebrew-mxcl-nginx`, and `named-target` → `profile:sentinel/named-target/system-shells`; no new persistent namespace is introduced.
</interfaces>
</context>

<execution_rules>
- Task-level functional suites use fresh external synthetic analogs. The default phase gate additionally performs only the five exact-manifest, pre-proven read-only outer safety sentinels; this is neither tool discovery nor `live-check`, never scans whole HOME/service space, and cannot fall back to an unallowlisted command.
- Manifest scope is immutable once before observation starts. Optional/excluded status cannot be changed after any snapshot.
- No retry, restore, destructive/convergence cleanup or attribution path may change a non-pass verdict. The sole teardown is the marker/UID/nonce/containment-verified external fixture child after primary verdict freeze; it can only preserve/worsen non-pass.
- Preserve unrelated dirty state; exact-file staging, scoped privacy review, English commits, no push.
</execution_rules>

<tasks>

<task type="auto" tdd="true">
  <name>Task 01-05-01: 校验 protected manifest 并取得 bounded opaque snapshots</name>
  <files>safety/internal/sentinel/manifest.go, safety/internal/sentinel/snapshot.go, safety/internal/sentinel/sentinel_test.go, safety/manifests/protected-surfaces.v1.json, safety/cmd/yamc-safety/main.go</files>
  <read_first>
    - AGENTS.md
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - .planning/phases/01-safety-privacy-and-state-foundation/01-CONTEXT.md
    - .planning/phases/01-safety-privacy-and-state-foundation/01-RESEARCH.md
    - safety/internal/sentinel/synthetic.go
    - safety/internal/fixture/root.go
    - safety/internal/privacy/gate.go
    - safety/cmd/yamc-safety/main.go
    - safety/internal/sentinel/manifest.go (new; confirm absent before creation)
    - safety/internal/sentinel/snapshot.go (new; confirm absent before creation)
    - safety/internal/sentinel/sentinel_test.go (new; confirm absent before creation)
    - safety/manifests/protected-surfaces.v1.json (new; confirm absent before creation)
  </read_first>
  <behavior>
    - Manifest accepts exactly the five D-13 surface domains with logical IDs, required/optional/excluded status, adapter ID and finite file/byte/time bounds.
    - Every surface ID must match the closed D-08 namespace/domain compatibility table; wrong-domain namespace, unknown namespace/domain, traversal, absolute suffix and resolver escape fail before snapshot, persistence or rendering.
    - Unknown/duplicate surface, physical path, missing bound, post-start manifest mutation, whole-HOME/service wildcard and unsupported required adapter fail closed.
    - Synthetic snapshots compare type, existence, mode, symlink identity and bounded content/tree state, not only mtime or item count.
    - Unreadable/race/overflow/symlink-escape/window failures produce typed incomplete status and never a token that can pass.
  </behavior>
  <action>
Write failing manifest/snapshot/CLI tests first. Implement D-13 with a closed manifest for `worktree`, `named-home`, `manager-root`, `service` and `named-target`, each using logical IDs and a privacy-safe adapter ID plus explicit required/optional/excluded status and bounds. Reuse D-08's six-namespace registry and require the exact compatibility table: worktree refs `repo:sentinel/worktree/tracked` and `repo:sentinel/worktree/index`; named-home ref `home:.zshrc`; manager-root ref `home:sentinel/manager/mise-data`; service ref `profile:sentinel/service/homebrew-mxcl-nginx`; named-target ref `profile:sentinel/named-target/system-shells`. Reject wrong-domain namespace, legacy/unknown namespace, absolute suffix, traversal, resolver escape, whole-HOME/service wildcard, duplicates, unknown domains/adapters, missing bounds and post-start manifest mutation before any snapshot/persistence/render. Implement injected synthetic adapters that model all five domains under the external fixture using the same public refs; do not call a real service/defaults/manager/HOME query in these task-level suites. Snapshot bounded state with per-run HMAC and return incomplete for unreadable/race/cap/symlink/window failures. Extend `sentinel verify` to output only compatible logical refs and opaque tokens/status. Stage exactly the five files and create English commit `feat(01-05): scope protected surface snapshots` after scoped privacy review.
  </action>
  <verify>
    <automated>./safety/scripts/test.sh task sentinel-manifest</automated>
  </verify>
  <acceptance_criteria>
    - The tracked manifest contains all five required real domain types using exactly the compatibility-table refs and no physical identity/path; synthetic blueprint manifests remain separate test doubles.
    - Cross-product negatives prove wrong-domain namespace, unknown namespace/domain, traversal, absolute suffix and resolver escape fail before snapshot/store/stdout/stderr.
    - Required/optional/excluded and bounds are fixed before snapshot; crafted post-start changes fail.
    - Replacement with equal mtime/count is detected; unreadable/race/overflow/symlink escape is incomplete rather than unchanged.
    - Automated tests resolve every domain only beneath an external fixture and invoke no real service/defaults/manager command.
    - Snapshot output contains no filename/content/path input or stable cross-run fingerprint.
  </acceptance_criteria>
  <done>The operator can validate an exact public protected-surface manifest and obtain bounded, privacy-safe snapshots; synthetic analogs remain explicitly non-claiming test doubles.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 01-05-02: 强制四态 verdict、non-pass exits 与 scoped claim</name>
  <files>safety/internal/sentinel/verdict.go, safety/internal/sentinel/verdict_test.go, safety/internal/workflow/synthetic.go, safety/cmd/yamc-safety/main.go, safety/internal/e2e/sentinel_cli_test.go</files>
  <read_first>
    - AGENTS.md
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - .planning/phases/01-safety-privacy-and-state-foundation/01-CONTEXT.md
    - .planning/phases/01-safety-privacy-and-state-foundation/01-RESEARCH.md
    - safety/internal/sentinel/manifest.go
    - safety/internal/sentinel/snapshot.go
    - safety/internal/workflow/synthetic.go
    - safety/cmd/yamc-safety/main.go
    - safety/internal/sentinel/verdict.go (new; confirm absent before creation)
    - safety/internal/sentinel/verdict_test.go (new; confirm absent before creation)
    - safety/internal/e2e/sentinel_cli_test.go (new; confirm absent before creation)
  </read_first>
  <behavior>
    - Complete/equal required evidence yields only `passed`; required difference yields `violation`; missing/limited/unavailable observation yields `indeterminate`; manifest/schema/internal invariant failure yields `harness-error`.
    - The latter three are non-zero; only predeclared optional incomplete/difference may warn without changing a complete required pass.
    - A required difference reports only `change-detected-during-window`, never attribution, auto-restore, ignore, retry or post-hoc optional conversion.
    - Passed evidence binds exact suite, tier, manifest digest, observation window, before/after tokens and optional/excluded list; claim is exactly `covered-surfaces-unchanged-for-run`.
    - Synthetic-only evidence emits `synthetic-sentinel-passed` for internal test status and is structurally unable to emit the real-surface success claim.
  </behavior>
  <action>
Write failing verdict/evidence/CLI tests first. Implement D-14 as the exact four-state enum and deterministic non-zero exit classes for `violation`, `indeterminate` and `harness-error`; require every required surface to have parseable, same-manifest, closed-window before/after evidence for `passed`. Honor optional warnings only when optional was fixed before observation. Implement D-15 so any required difference immediately becomes `violation` with only `change-detected-during-window`; do not add attribution, auto-restore, auto-ignore, retry-to-pass or post-hoc exclusion. Implement D-16 evidence fields binding exact suite ID/digest, test tier, protected manifest digest, observation window, required tokens and optional/excluded list. Preserve Plan 01-01's already-compatible contract: inner test doubles emit only `synthetic-sentinel-passed`, while the renderer requires complete outer real evidence before first emitting `covered-surfaces-unchanged-for-run` and rejects whole-Mac, current-host readiness, multi-host and fresh-install claims. Keep after-attempt failures monotonic and non-masking; do not rewrite or weaken `walking_skeleton_test.go`. Stage exactly the five files and create English commit `feat(01-05): enforce sentinel verdicts` after scoped privacy review.
  </action>
  <verify>
    <automated>./safety/scripts/test.sh task sentinel-verdicts</automated>
  </verify>
  <acceptance_criteria>
    - Tests cover all four verdicts and verify exact exit behavior; no missing required evidence can return 0.
    - Optional warnings pass only when every required sentinel is complete/equal and optional status predates the window.
    - Required changes never cause restore/retry/ignore/attribution and expose only the bounded change code.
    - Evidence includes every D-16 binding and rejects manifest/tier/window/token substitution.
    - The only real-surface success claim accepted/rendered is `covered-surfaces-unchanged-for-run`; Wave 1's synthetic path remains `synthetic-sentinel-passed` without modifying its E2E contract.
    - Synthetic-only evidence cannot select or render that claim, including through crafted mode/provenance fields.
  </acceptance_criteria>
  <done>The operator receives a strict, evidence-bound sentinel verdict whose success claim is honest about the exact run and covered surfaces.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 01-05-03: 以已证明的精确只读 adapters 包住真实 protected surfaces</name>
  <files>safety/internal/sentinel/real.go, safety/internal/sentinel/real_test.go, safety/manifests/real-adapters.v1.json, safety/manifests/protected-surfaces.v1.json, safety/cmd/yamc-safety/main.go, safety/internal/e2e/real_sentinel_cli_test.go</files>
  <read_first>
    - AGENTS.md
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - .planning/phases/01-safety-privacy-and-state-foundation/01-CONTEXT.md
    - .planning/phases/01-safety-privacy-and-state-foundation/01-RESEARCH.md
    - safety/internal/sentinel/manifest.go
    - safety/internal/sentinel/snapshot.go
    - safety/internal/sentinel/verdict.go
    - safety/internal/fixture/retention.go
    - safety/manifests/protected-surfaces.v1.json
    - safety/internal/sentinel/real.go (new; confirm absent before creation)
    - safety/internal/sentinel/real_test.go (new; confirm absent before creation)
    - safety/manifests/real-adapters.v1.json (new; confirm absent before creation)
    - safety/internal/e2e/real_sentinel_cli_test.go (new; confirm absent before creation)
  </read_first>
  <behavior>
    - The default phase gate requires complete real-before and real-after evidence for all five domains using `repo:sentinel/worktree/{tracked,index}`, `home:.zshrc`, `home:sentinel/manager/mise-data`, `profile:sentinel/service/homebrew-mxcl-nginx`, and `profile:sentinel/named-target/system-shells`; missing mapping/proof/adapter is non-zero `indeterminate`/`manual-required`.
    - Each exact adapter/version is allowlisted only while current official read-only semantics and the exact isolated negative-suite digest are both valid; synthetic adapters can never satisfy this gate.
    - Real adapters expose only logical IDs and per-run opaque tokens, use finite file/byte/time/process bounds, never enumerate whole HOME/services, and retain roots, UID, identity, raw output and HMAC key only in process memory.
    - Ordering is fixed: real-before → isolated workload with inner synthetic sentinels → freeze primary verdict → marker-owned fixture teardown unless pre-run keep → real-after → monotonic final combine.
  </behavior>
  <action>
Write failing registry/adapter/E2E tests first. Record for each exact adapter ID/version its fixed Go API or executable/argv, official source and reviewed read-only claim, `reviewed_at`, at-most-30-day `valid_until`, isolated negative-suite ID/digest, strict statuses and resource bounds. Implement only: Git worktree/index for `repo:sentinel/worktree/{tracked,index}` via fixed `git -c core.fsmonitor=false status --porcelain=v2 --untracked-files=no` plus `git ls-files -z --stage` and a bounded lstat/content fingerprint of exactly that tracked set and index, all with `GIT_OPTIONAL_LOCKS=0`; exact non-secret `home:.zshrc` via bounded Go `Lstat`/fingerprint; named manager root `home:sentinel/manager/mise-data` via bounded tree fingerprint; controlled service `profile:sentinel/service/homebrew-mxcl-nginx` via `launchctl print gui/<uid>/homebrew.mxcl.nginx` with strict absent mapping; and named target `profile:sentinel/named-target/system-shells` via exact `/etc/shells` bounded Go fingerprint. Validate domain compatibility before resolving; keep physical roots, UID, host identity, service-query output and resolver mappings in memory, HMAC bounded observations with a per-run key, discard inputs, and emit compatible refs only. Missing/stale proof, failed/mismatched negative suite, wrong-domain ref, resolver escape, cap/race/unreadable state, unexpected status or unavailable mapping is non-zero `indeterminate`/`manual-required`, never synthetic fallback. Preserve the fixed outer sequence and monotonic teardown/after combine. Stage exactly the six `<files>` paths—and no directory or wildcard—run cached-diff checks, targeted identity/path/raw-output scans and staged Gitleaks over that exact whitelist, then create English commit `feat(01-05): guard real surfaces with sentinels`.
  </action>
  <verify>
    <automated>./safety/scripts/test.sh task real-sentinel-envelope</automated>
  </verify>
  <acceptance_criteria>
    - Tests require all five domains and their exact compatibility-table refs (including both tracked-worktree/index sub-IDs), complete equal before/after pairs and both fresh official-semantics plus exact negative-evidence proof before the scoped claim is possible.
    - Missing/stale proof, unsupported status, incomplete observation, adapter substitution and synthetic-only evidence are non-zero and cannot render the claim.
    - Process/raw/root/UID/HMAC data never enters public artifacts, stdout/stderr or tracked fixtures; no whole HOME/service scan or unallowlisted command exists.
    - Success-teardown, failure-teardown and pre-run keep cases enforce the exact sequence; teardown/after failure never improves the frozen primary verdict.
    - `git diff --cached --name-only` equals exactly these six paths: `safety/internal/sentinel/real.go`, `safety/internal/sentinel/real_test.go`, `safety/manifests/real-adapters.v1.json`, `safety/manifests/protected-surfaces.v1.json`, `safety/cmd/yamc-safety/main.go`, `safety/internal/e2e/real_sentinel_cli_test.go`; no directory/wildcard staging is allowed.
    - `git diff --cached --check --` over those exact six paths passes; a targeted staged scan finds no real username, expanded UID/host identity, physical resolver root or retained raw service output (the reviewed fixed public `/etc/shells` literal is the only physical target literal); `gitleaks git --staged --redact --no-banner` passes after the exact whitelist assertion.
  </acceptance_criteria>
  <done>The default offline phase gate can make its scoped unchanged claim only from a complete, pre-proven outer real-surface observation window.</done>
</task>

</tasks>

<threat_model>
## ASVS L1 Security Posture

Auditability, integrity of security evidence, bounded resource consumption, safe failure and prevention of misleading success are blocking ASVS L1 controls. Any unresolved HIGH threat blocks this plan.

## Trust Boundaries

| Boundary | Untrusted crossing | Control |
|---|---|---|
| Manifest → adapter registry | domain/ID/status/bounds | closed schema, logical IDs, digest frozen before window |
| Surface → snapshot | synthetic and exact real filesystem/service state | bounded allowlisted adapters, proof freshness and opaque per-run HMAC |
| Snapshot pair → verdict | missing/different/malformed evidence | strict four-state evaluator |
| Verdict → report | claim/evidence fields | passed-only report and exact claim allowlist |

## STRIDE Threat Register

| Threat ID | STRIDE | Severity | Component | Disposition | Concrete mitigation and blocking proof |
|---|---|---:|---|---|---|
| T-01 | Spoofing / Tampering | HIGH | manifest/evidence identity | mitigate | Exact manifest/suite/tier/window digests and token pairing; substitution tests block completion. |
| T-02 | Information Disclosure | HIGH | surface snapshots | mitigate | Logical IDs, bounded adapters, per-run opaque HMAC and no raw path/name/content output. |
| T-03 | Tampering / Elevation of Privilege | HIGH | sentinel adapters | mitigate | Exact adapter/version registry requires current official read-only semantics plus isolated negative evidence; no arbitrary dispatch, discovery or mutation. |
| T-04 | Tampering / Elevation of Privilege | HIGH | symlink/tree observation | mitigate | Canonical fixture containment, bounded lstat-style state and symlink-escape→indeterminate. |
| T-05 | Elevation of Privilege / Information Disclosure | HIGH | real observation boundary | mitigate | Exact finite adapters keep roots/UID/raw state in memory; missing/stale proof is indeterminate/manual-required and never falls back or escalates. |
| T-06 | Repudiation / Denial of Service | HIGH | verdict/claim | mitigate | D-14..D-16 four-state non-pass exits, exact evidence binding, resource caps and overclaim rejection. |

## Security Gate

Any incomplete required evidence returning pass, post-hoc optional/exclusion, retry/restore behavior, stable machine fingerprint or stronger claim is an unresolved HIGH threat and blocks all three commits.
</threat_model>

<verification>
- `./safety/scripts/test.sh task sentinel-manifest`
- `./safety/scripts/test.sh task sentinel-verdicts`
- `./safety/scripts/test.sh task real-sentinel-envelope`
- `./safety/scripts/test.sh wave sentinels`
- `git diff --check -- safety/internal/sentinel/manifest.go safety/internal/sentinel/snapshot.go safety/internal/sentinel/sentinel_test.go safety/manifests/protected-surfaces.v1.json safety/cmd/yamc-safety/main.go safety/internal/sentinel/verdict.go safety/internal/sentinel/verdict_test.go safety/internal/workflow/synthetic.go safety/internal/e2e/sentinel_cli_test.go safety/internal/sentinel/real.go safety/internal/sentinel/real_test.go safety/manifests/real-adapters.v1.json safety/internal/e2e/real_sentinel_cli_test.go`
</verification>

<success_criteria>
- SAFE-06/SAFE-07 have synthetic contract evidence plus a default outer exact-manifest real read-only envelope for all five D-13 domains; this envelope is not a live-check.
- D-13～D-16 are enforced in manifest, snapshot, verdict, evidence, CLI exit and claim behavior.
- All required non-pass cases remain non-zero and cannot be softened after observation begins.
- Three exact-file English commits are created and not pushed.
</success_criteria>

<output>
Create `.planning/phases/01-safety-privacy-and-state-foundation/01-05-SUMMARY.md` when done.
</output>
