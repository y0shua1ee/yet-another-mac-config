---
phase: 01-safety-privacy-and-state-foundation
plan: "02"
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - safety/internal/artifact/envelope.go
  - safety/internal/artifact/kinds.go
  - safety/internal/artifact/canonical.go
  - safety/internal/artifact/validate_test.go
  - safety/testdata/artifacts/kind-cases.json
  - safety/internal/artifact/lineage.go
  - safety/internal/artifact/store.go
  - safety/cmd/yamc-safety/main.go
  - safety/internal/e2e/artifact_cli_test.go
  - safety/testdata/artifacts/lineage-cases.json
autonomous: true
requirements:
  - SAFE-01
must_haves:
  truths:
    - "D-01：六类 artifact 共享 envelope，但每个 kind 进入独立、封闭、拒绝未知字段的 payload validator，不能互相冒充。"
    - "D-02：tracked Git 只容纳 public desired sources 与 validated synthetic golden；所有 runtime/real-run canonical artifacts 只能进入 external-local-state，并按 kind 执行封闭 TTL、terminal state、append-only、transitive pin 与 delete policy。"
    - "D-03：kind、schema、version、provenance、storage class 或 lifecycle policy 失败会整份拒绝、non-zero、零持久化，且不会产生 quarantine 或 degraded artifact。"
    - "D-04：plan、receipt、evidence 与 report 只通过重新验证的 exact digests 连接；run_id、文件名、mtime、目录与 latest alias 均无完整性权限。"
  artifacts:
    - path: "safety/internal/artifact/kinds.go"
      provides: "Six kind-specific closed payload contracts"
      contains: "readiness-report"
    - path: "safety/internal/artifact/canonical.go"
      provides: "Restricted canonical JSON and duplicate-key rejection"
      contains: "Canonical"
    - path: "safety/internal/artifact/lineage.go"
      provides: "Apply and read-only exact digest graph validation"
      contains: "Lineage"
    - path: "safety/testdata/artifacts/lineage-cases.json"
      provides: "Synthetic substitution, stale and latest-selection negative cases"
      contains: "invalid"
  key_links:
    - from: "safety/cmd/yamc-safety/main.go"
      to: "safety/internal/artifact/kinds.go"
      via: "validate --expect-kind"
      pattern: "Validate"
    - from: "safety/internal/artifact/store.go"
      to: "safety/internal/artifact/lineage.go"
      via: "validate entire graph before store/read"
      pattern: "ValidateLineage"
---

## Phase Goal

**As a** Mac 配置仓库操作者，**I want to** 校验严格区分且有 lineage 的恢复 artifacts，并在隔离 fixture 中运行端到端安全测试，**so that** 我能证明本次运行覆盖的真实 Mac 表面没有发生未经授权的变化。

<objective>
把 walking skeleton 的 valid happy path 收紧为 closed six-kind contract 与 exact lineage validator，使 `validate`/`store` 能机械拒绝错误类型、错误 version、未知字段、digest substitution、stale edge 和 `latest` 拼接。

Purpose: 完整落实 SAFE-01 与 D-01～D-04，使任何 artifact 都不能以相似字段、run ID 或目录位置冒充另一种证据。
Output: kind-specific schemas、restricted canonical JSON、lineage graph、store read/write enforcement、CLI negative paths 与 synthetic golden cases。
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-safety-privacy-and-state-foundation/01-CONTEXT.md
@.planning/phases/01-safety-privacy-and-state-foundation/01-RESEARCH.md
@.planning/phases/01-safety-privacy-and-state-foundation/01-PATTERNS.md
@.planning/phases/01-safety-privacy-and-state-foundation/01-VALIDATION.md
@.planning/phases/01-safety-privacy-and-state-foundation/01-01-SUMMARY.md

<interfaces>
- Reuse `artifact.Envelope`, `artifact.Kind`, `artifact.Store` and `workflow.RunSynthetic` created by Plan 01-01; do not create a second envelope/store path.
- Add `artifact.Validate(expectedKind, canonicalBytes)`, `artifact.Canonicalize`, `artifact.ValidateLineage(mode, graph)` and stable typed error codes.
- `lineage_mode` has only `apply` and `read-only`; read-only cannot contain or imply an applied receipt.
- `artifact.Store` recognizes exactly two storage classes: tracked `synthetic-golden` is read-only and requires `provenance.mode=synthetic`; `external-local-state` is the only writable class and is mandatory for every runtime/real-run canonical artifact.
- Closed D-02 lifecycle table: desired-state and observed-state are rebuildable snapshots with an explicit 24-hour expiry while unpinned; generated-plan is immutable/append-only and nonterminal until an explicit `applied` or `abandoned` transition; applied-receipt, verification-evidence and readiness-report are append-only evidence-bundle members that pin every transitive ancestor and are never auto-pruned or deleted in Phase 1.
</interfaces>
</context>

<execution_rules>
- Use only the existing local/offline runner. No package install, Go auto-download, network, live probe, Nix/Homebrew/manager command or host mutation.
- Invalid input must be rejected before any store write or renderer output; never persist raw invalid input or a quarantine copy.
- Preserve unrelated dirty state. Each task stages its exact files only, performs scoped privacy review, commits in English, and does not push.
</execution_rules>

<tasks>

<task type="auto" tdd="true">
  <name>Task 01-02-01: 封闭六类 payload 与 canonical envelope</name>
  <files>safety/internal/artifact/envelope.go, safety/internal/artifact/kinds.go, safety/internal/artifact/canonical.go, safety/internal/artifact/validate_test.go, safety/testdata/artifacts/kind-cases.json</files>
  <read_first>
    - AGENTS.md
    - .planning/REQUIREMENTS.md
    - .planning/phases/01-safety-privacy-and-state-foundation/01-CONTEXT.md
    - .planning/phases/01-safety-privacy-and-state-foundation/01-RESEARCH.md
    - .planning/phases/01-safety-privacy-and-state-foundation/01-PATTERNS.md
    - safety/internal/artifact/envelope.go
    - safety/internal/artifact/kinds.go (new; confirm absent before creation)
    - safety/internal/artifact/canonical.go (new; confirm absent before creation)
    - safety/internal/artifact/validate_test.go (new; confirm absent before creation)
    - safety/testdata/artifacts/kind-cases.json (new; confirm absent before creation)
  </read_first>
  <behavior>
    - Each of the six kinds accepts its own valid synthetic payload and rejects a payload from every other kind.
    - Unknown kind/version/field, duplicate JSON key, missing provenance, float/NaN-like number and caller-supplied digest mismatch reject the whole artifact.
    - Canonicalization sorts object keys, preserves array order, uses UTF-8 and produces the same digest for semantically identical allowed JSON.
    - Rejection emits a stable code and logical pointer but never stores the invalid bytes or returns partial/degraded success.
    - The closed kind registry assigns exactly one allowed lifecycle policy and storage class set to each kind; callers cannot invent retention modes, TTLs or terminal states.
  </behavior>
  <action>
Write failing table-driven tests first, then implement D-01 with a closed registry of exactly six typed payload decoders behind the common envelope. Use `json.Decoder.DisallowUnknownFields` plus an explicit token walk that detects duplicate keys; reject unknown kind/version, trailing tokens, non-integer numeric forms, malformed UTF-8 semantics and incomplete provenance before constructing a valid envelope. Implement restricted canonical JSON and recompute `content_digest` over every envelope field except the digest itself. Per D-03, return stable typed errors, reject the entire artifact, and expose no partial payload, degraded success, raw diagnostic or quarantine write. Encode D-02 as a closed per-kind table rather than prose or caller input: desired-state/observed-state use `snapshot-24h` and require a concrete `expires_at=created_at+24h`; generated-plan uses `append-only-plan` and records only `nonterminal`, `applied` or `abandoned`; applied-receipt/verification-evidence/readiness-report use `append-only-evidence-bundle`. Map tracked `synthetic-golden` only to validated `provenance.mode=synthetic` fixtures and make that class read-only; map every runtime/real-run artifact to writable `external-local-state`. Reject any unknown retention name, caller-selected TTL, wrong class-kind pair or impossible terminal state before persistence. Keep all cases synthetic in one reviewed table file. Stage exactly these five files and create English commit `feat(01-02): enforce artifact kind contracts` after scoped privacy review.
  </action>
  <verify>
    <automated>./safety/scripts/test.sh task artifact-kinds</automated>
  </verify>
  <acceptance_criteria>
    - The validator registry contains exactly the six SAFE-01 kinds and has no catch-all payload/map path.
    - Every decoder rejects unknown fields and cross-kind payload substitution; duplicate keys are rejected before canonicalization.
    - Digest calculation excludes only `content_digest`; array order remains significant and object key order does not.
    - Every negative case returns non-zero with stable code/logical pointer and leaves the external store unchanged.
    - The golden-case file contains only synthetic logical refs and no real identity/path/provider/network material.
    - Tests cover every valid class-kind pair plus wrong-class, unsupported-retention, non-24-hour snapshot TTL, invalid plan terminal state and non-synthetic tracked-golden rejection.
  </acceptance_criteria>
  <done>The CLI data plane can distinguish and strictly validate each artifact kind before any artifact is considered valid or writable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 01-02-02: 强制 exact digest lineage 与 immutable store reads</name>
  <files>safety/internal/artifact/lineage.go, safety/internal/artifact/store.go, safety/cmd/yamc-safety/main.go, safety/internal/e2e/artifact_cli_test.go, safety/testdata/artifacts/lineage-cases.json</files>
  <read_first>
    - AGENTS.md
    - .planning/phases/01-safety-privacy-and-state-foundation/01-CONTEXT.md
    - .planning/phases/01-safety-privacy-and-state-foundation/01-RESEARCH.md
    - safety/internal/artifact/envelope.go
    - safety/internal/artifact/kinds.go
    - safety/internal/artifact/canonical.go
    - safety/internal/artifact/lineage.go (new; confirm absent before creation)
    - safety/internal/artifact/store.go
    - safety/cmd/yamc-safety/main.go
    - safety/internal/e2e/artifact_cli_test.go (new; confirm absent before creation)
    - safety/testdata/artifacts/lineage-cases.json (new; confirm absent before creation)
  </read_first>
  <behavior>
    - Apply lineage requires exact desired+observed→plan, plan→receipt, plan+receipt+expected-postconditions+fresh-observed→evidence, and evidence→report edges.
    - Read-only lineage is explicitly labeled, binds fresh observation/expected postconditions, and rejects any receipt substitution.
    - `store` recomputes the digest on read and write, rejects key/content mismatch and refuses overwrite with different bytes.
    - `run_id`, filename, mtime, directory co-location, unverified caller digest and `latest` aliases never select or validate an upstream artifact.
    - Validate, write, read and delete all re-check the same closed class-kind-retention policy; an expired unpinned snapshot cannot be used, while a pinned snapshot remains readable until its pin is released.
    - Evidence-bundle members pin every transitive ancestor; Phase 1 refuses deletion of any evidence-bundle member, nonterminal plan, pinned artifact or unsupported lifecycle class.
  </behavior>
  <action>
Write failing lineage/store/CLI tests first. Implement D-04 as a schema-defined graph with separate apply and read-only modes. Require generated plans to bind exact desired and observed digests; require receipts to bind the exact plan and report only operation IDs present in plan order; require apply evidence to bind plan, receipt, expected-postconditions and a separately stored fresh observation; require reports to list only evidence they actually load and validate. Reject missing, extra, substituted, stale or reordered edges and prohibit `latest`, directory guessing, mtime and filename selection. Complete D-02 in the store: Validate/Write/Read/Delete must all re-resolve the closed class-kind-retention table; exact duplicate content-addressed writes may return the existing handle without rewriting bytes, while key collision/different bytes and every truncate/replace path fail. A desired/observed snapshot expires exactly 24 hours after creation only while unpinned; any nonterminal plan or evidence bundle referencing it pins it, and an expired pinned snapshot remains readable until the last pin is released. Generated plans are append-only and cannot be deleted before an explicit terminal transition: `applied` must bind an exact applied-receipt for that plan, while `abandoned` must be an explicit terminal record. Applied-receipt, verification-evidence and readiness-report form append-only bundles, recursively pin every desired/observed/plan/receipt/evidence ancestor, and have no delete or auto-prune operation in Phase 1. Delete may remove only expired unpinned desired/observed snapshots or terminal unpinned plans; reject premature deletion, pinned deletion, expired-unpinned reads, wrong storage class and unsupported retention. Extend CLI `validate --expect-kind` and `store` routes so all success output is bounded and all invalid graphs fail before writes. Stage exactly these five files, run scoped privacy review, and create English commit `feat(01-02): bind exact artifact lineage`.
  </action>
  <verify>
    <automated>./safety/scripts/test.sh task artifact-lineage</automated>
  </verify>
  <acceptance_criteria>
    - A valid apply chain and valid read-only chain pass; every wrong-kind, stale, substituted, missing, extra and reordered edge in the fixture fails.
    - Changing only `run_id` never repairs a broken digest edge; matching directory/filename/mtime never influences selection.
    - An evidence artifact that reuses the pre-apply observation or omits expected-postconditions fails.
    - Store reads fail when content no longer hashes to the key; a plan key cannot be overwritten with different canonical bytes.
    - `validate` and `store` have no fallback to scan a directory or choose a newest artifact.
    - Lifecycle tests cover valid writes/reads, wrong class-kind pair, expired unpinned use, exact duplicate no-op, different-byte overwrite, nonterminal-plan deletion, pinned deletion and unsupported retention.
    - Evidence-bundle tests prove transitive ancestor pins, no Phase 1 evidence deletion/auto-prune, and exact `applied`/explicit `abandoned` plan terminal transitions.
  </acceptance_criteria>
  <done>All six artifact roles and their allowed relationships are mechanically distinct, content-addressed and fail closed under substitution or staleness.</done>
</task>

</tasks>

<threat_model>
## ASVS L1 Security Posture

Input validation, canonical serialization, integrity checks, safe error handling and deny-by-default state transitions are mandatory ASVS L1 controls. Any unresolved HIGH threat blocks execution.

## Trust Boundaries

| Boundary | Untrusted crossing | Control |
|---|---|---|
| JSON bytes → envelope | kind/version/fields/numbers | closed decoder, duplicate-key and trailing-token rejection |
| Envelope → canonical digest | caller-provided content | restricted canonical form and recomputed SHA-256 |
| Artifact graph → evidence | upstream digest references | schema-defined exact edges and mode-specific rules |
| Store path → content | filename/key claims | recompute on read/write; immutable plan key |

## STRIDE Threat Register

| Threat ID | STRIDE | Severity | Component | Disposition | Concrete mitigation and blocking proof |
|---|---|---:|---|---|---|
| T-01 | Spoofing / Tampering | HIGH | kind/schema/lineage/lifecycle | mitigate | Six closed payload decoders, closed class-kind-retention table, duplicate-key rejection, canonical digest and exact graph; cross-kind, wrong-class, unsupported-retention, stale and latest negative suites block completion. |
| T-02 | Information Disclosure | HIGH | validation errors | mitigate | Stable code and logical pointer only; invalid raw bytes are never stored or echoed. Full reusable privacy envelope arrives in Plan 01-03. |
| T-03 | Tampering / Elevation of Privilege | HIGH | generated-plan payload | mitigate | Data-only typed payload, no raw command fields, immutable plan keys, no apply route. |
| T-04 | Tampering / Elevation of Privilege | HIGH | artifact store filesystem | mitigate | Reuse Plan 01-01 external-root containment; store-key mismatch and overwrite tests are blocking. |
| T-05 | Elevation of Privilege / Information Disclosure | HIGH | validation runner | mitigate | Offline local Go environment only; no network, manager or live escalation path. |
| T-06 | Repudiation / Denial of Service | HIGH | evidence/report graph and retention | mitigate | Report lists exact validated evidence only; 24-hour snapshot expiry, plan terminal states and transitive pins are enforced on every operation; premature/pinned/evidence deletion and missing/freshness failures are non-zero. |

## Security Gate

All T-01..T-06 mitigations must have passing negative tests. Any partial artifact, quarantine write, key mismatch, latest selection or lineage ambiguity blocks both task commits.
</threat_model>

<verification>
- `./safety/scripts/test.sh task artifact-kinds`
- `./safety/scripts/test.sh task artifact-lineage`
- `./safety/scripts/test.sh wave artifact-contracts`
- `git diff --check -- safety/internal/artifact/envelope.go safety/internal/artifact/kinds.go safety/internal/artifact/canonical.go safety/internal/artifact/validate_test.go safety/testdata/artifacts/kind-cases.json safety/internal/artifact/lineage.go safety/internal/artifact/store.go safety/cmd/yamc-safety/main.go safety/internal/e2e/artifact_cli_test.go safety/testdata/artifacts/lineage-cases.json`
</verification>

<success_criteria>
- SAFE-01 has positive and negative CLI evidence for all six kinds and both allowed lineage modes.
- D-01～D-04 are implemented without a generic payload escape hatch, undefined lifecycle state, raw invalid persistence, partial success or latest-file discovery.
- Tracked Git contains only public desired sources and validated synthetic golden; every runtime/real-run canonical artifact is external-local-state, and reverse phase validation re-checks class-kind-retention, expiry, terminal state and all transitive pins.
- All tests remain external/offline and leave repo/HOME/live state unchanged.
- Two exact-file English commits are created and not pushed.
</success_criteria>

<output>
Create `.planning/phases/01-safety-privacy-and-state-foundation/01-02-SUMMARY.md` when done.
</output>
